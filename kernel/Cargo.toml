[package]
name = "cotton_kernel"
version = "0.1.0"
edition = "2021"
authors = ["aryansrao"]
description = "The CottonOS kernel"

# Build as static library for linking with boot stub
[lib]
name = "cotton_kernel"
path = "src/main.rs"
crate-type = ["staticlib"]

[dependencies]
spin = "0.9"
bitflags = "2.4"
volatile = "0.5"
linked_list_allocator = "0.10"
embedded-tls = { version = "0.18", default-features = false, features = ["alloc"] }
smoltcp = { version = "0.12", default-features = false, features = [
    "alloc",
    "medium-ethernet",
    "proto-ipv4",
    "proto-dhcpv4",
    "proto-dns",
    "socket-tcp",
    "socket-udp",
    "socket-dhcpv4",
    "socket-dns",
    "socket-icmp",
] }
embedded-io = "0.7"
rand_core = { version = "0.6", default-features = false }

[dependencies.lazy_static]
version = "1.4"
features = ["spin_no_std"]

[features]
default = []
x86_64 = []
aarch64 = []
test = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
